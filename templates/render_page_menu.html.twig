{% set requestedCode = menuCode|default(PAGE_NAV) %}
{% set resolvedType = requestedCode in ['survos_page_menu', 'PAGE_MENU'] ? PAGE_NAV : requestedCode %}

{{ component('tabler:menu', {
    type: resolvedType,
    caller: _self,
    options: {
        title: page_title|default(null)
    }
}) }}
